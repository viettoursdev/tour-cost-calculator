// Stub — full implementation in PR Menu Task 7.
import type { Menu } from '@/types';

export async function exportMenuDocx(_it: Menu, _code: string): Promise<void> {
  window.alert('Xuất Word sẽ có trong task tiếp theo.');
}
