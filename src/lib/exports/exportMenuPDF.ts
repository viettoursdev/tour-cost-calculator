// Stub — full implementation in PR Menu Task 8.
import type { Menu } from '@/types';

export function exportMenuPDF(_it: Menu, _code: string): void {
  window.alert('Xuất PDF sẽ có trong task tiếp theo.');
}
