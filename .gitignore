.DS_Store
node_modules
dist
*.log
*.tsbuildinfo
vite.config.js
vite.config.d.ts
.playwright-mcp/
vite-after-mo-click.png

# Service-account keys — contain credentials. Never commit.
old-service-account.json
new-service-account.json
prod-sa.json
*-sa.json

# Local env files — keep .env.example committed as the template.
.env
.env.local
.env.*.local

# Transient migration scratch notes — never commit.
MIGRATION_NOTES.md

# Supabase local dev — transient dirs, never commit.
supabase/.branches
supabase/.temp
.supabase_db_password
prod-sa.json
.supabase_service_key
