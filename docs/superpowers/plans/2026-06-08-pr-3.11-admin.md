# PR-3.11 Admin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:executing-plans`.

**Goal:** Port the final two legacy admin modals (UserManagement + RateCardSync) and wire them via a settings menu in the AppShell AppBar.

**Spec:** `docs/superpowers/specs/2026-06-08-pr-3.11-admin-design.md`

**Tech:** TS · React 18 · MUI v6 · Zustand · Firebase.

**Tests:** Out of scope. Verify via `npm run typecheck && npm run lint && npm run build`. V-checklist runs in the browser.

**Conventions per task:**
- `npm run typecheck && npm run lint` after every change.
- Commit per task; Conventional Commits.
- Co-author: `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.

---

## Task 1: ROLES + USER_COLORS constants

**Files:**
- Create: `src/auth/ROLES.ts`

- [ ] **Step 1:** Export `ROLES: readonly Role[]` matching the `Role` union order (`['CEO','Trưởng Phòng','Sales','Operations','Marketing','Admin','Accountant','Standard']`).
- [ ] **Step 2:** Export `USER_COLORS: readonly string[]` with the 10 legacy colors from line 5126: `['#dc3250','#f5a623','#14a08c','#1abc9c','#3498db','#9b59b6','#e67e22','#27ae60','#16a085','#8e44ad']`.
- [ ] **Step 3:** Export `DEFAULT_USERS: readonly User[]` mirroring the seed list inside `src/stores/authStore.ts` (so "Reset mặc định" doesn't depend on a store internal). Leave the store's own constant in place — duplication is intentional and small.
- [ ] **Step 4:** `npm run typecheck && npm run lint`.
- [ ] **Commit:** `feat(admin): ROLES + USER_COLORS + DEFAULT_USERS (PR-3.11 Task 1)`

---

## Task 2: UserManagementModal

**Files:**
- Create: `src/components/admin/UserManagementModal.tsx`

- [ ] **Step 1:** Props per spec §4. Read users from `useAuthStore`.
- [ ] **Step 2:** Header (red gradient), action row (Add + Reset).
- [ ] **Step 3:** Form panel with username/password row (username disabled when editing), name + role row, color picker (10 chips), Save/Cancel.
- [ ] **Step 4:** User list — avatar circle + name + role chip + "Đang đăng nhập" chip on self + Edit/Delete buttons.
- [ ] **Step 5:** Validation per spec §4 (alerts on empty fields / collisions / last-CEO-delete / self-delete).
- [ ] **Step 6:** Permission matrix at the bottom — table of 7 primary permissions × 8 roles, sourced from `PERMISSIONS`.
- [ ] **Step 7:** All mutations call `useAuthStore.getState().saveUsers(next)`.
- [ ] **Step 8:** `npm run typecheck && npm run lint`.
- [ ] **Commit:** `feat(admin): UserManagementModal — CRUD users + permission matrix (PR-3.11 Task 2)`

---

## Task 3: RateCardSyncModal

**Files:**
- Create: `src/components/admin/RateCardSyncModal.tsx`

- [ ] **Step 1:** Props per spec §5. State per §5.
- [ ] **Step 2:** Header (purple gradient) + 3-tab strip.
- [ ] **Step 3:** On `open === true`, mount a `useEffect` that runs `fbPullMasterRC()` (sets `cloudData`, `cloudLoading=false`) and subscribes via `fbSubscribeMasterRC`. Cleanup unsubscribes.
- [ ] **Step 4:** **Cloud tab** — cloud stats card (3 metrics + meta line), local stats card, status alert, action buttons (Push gated by `syncRateCard`, Pull disabled while loading/no data).
- [ ] **Step 5:** Push handler — `confirm(...)` → `setPushing(true)` → `fbPushMasterRC(rates, '{name} ({role})')` directly (bypass debouncer) → success message. Catch + error message.
- [ ] **Step 6:** Pull handler — `confirm(...)` → `setPulling(true)` → `useRateCardStore.setState({ rates: cloudData })` → success message → `setTimeout(() => window.location.reload(), 1200)`.
- [ ] **Step 7:** **Export tab** — local stats card + "📤 Xuất file" button that builds payload `{_meta, hotels, visaRates, otherRates}` and triggers download via `Blob` + `<a download>`. Filename: `Viettours_RateCard_Master_${todayISO}.json`.
- [ ] **Step 8:** **Import tab** — warning alert, file picker, preview card after file parses, Apply handler that confirms + sets store + reloads.
- [ ] **Step 9:** Validation — `_meta.type === 'viettours_ratecard_master'` else `alert('⚠ File không phải Master Rate Card hợp lệ')`.
- [ ] **Step 10:** `npm run typecheck && npm run lint`.
- [ ] **Commit:** `feat(admin): RateCardSyncModal — 3-tab cloud/export/import (PR-3.11 Task 3)`

---

## Task 4: Wire settings menu in AppShell

**Files:**
- Modify: `src/components/shell/AppShell.tsx`

- [ ] **Step 1:** Add `useState` for `settingsAnchor`, `userMgrOpen`, `rateSyncOpen`.
- [ ] **Step 2:** Insert the settings `IconButton` + `Menu` between `NotificationBell` and the `Đăng xuất` button per spec §6.
- [ ] **Step 3:** Mount both modals at the end of the AppShell return tree.
- [ ] **Step 4:** `npm run typecheck && npm run lint && npm run build`.
- [ ] **Commit:** `feat(admin): wire settings menu + modal mounts in AppShell (PR-3.11 Task 4)`

---

## Task 5: Full manual verification

Run V1–V14 from spec §9 in `npm run dev`. Commit any UX fixes as `fix(admin): …`.
