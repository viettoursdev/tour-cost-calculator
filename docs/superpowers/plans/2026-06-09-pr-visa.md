# PR-Visa Implementation Plan

**Goal:** Port "Quản lý Visa" feature (catalog + procedures) into the Vite app.

**Spec:** `docs/superpowers/specs/2026-06-09-pr-visa-design.md`

**Tech:** TS · React 18 · MUI v6 · Zustand · Firebase · docx · jspdf.

**Tests:** Out of scope. Verify each task via `typecheck + lint + build`.

**Conventions:** Commit per task. `feat(visa): …`. Co-author trailer.

---

## Task 1: Types + constants + factories

**Files:**
- Create: `src/types/visa.ts`
- Modify: `src/types/index.ts`
- Create: `src/components/visa/constants.ts`

- [ ] Write `visa.ts` per spec §4.
- [ ] Constants: `VISAP_TYPES`, `VISA_VALIDITY`, `VISA_LOCS`, `VISA_FEE_PRESET`, `PROC_KIND_ICON`. Factories: `newVisaFee`, `newVisaProduct`, `newProcField`, `newProcRow`, `newProcSection`, `newVisaProcDoc(user)`, `generateVisaProcCode`. Port from legacy 7573-7578 + 7935-7955.
- [ ] Commit: `feat(visa): types + constants + factories (PR Visa Task 1)`

---

## Task 2: Firestore signatures

**Files:**
- Modify: `src/lib/firebase.ts`

- [ ] Add `VISA_PRODUCTS_DOC`, `VISA_PROC_INDEX_DOC`, `visaProcDoc(id)`.
- [ ] Implement `fbSubscribeVisaProducts`, `fbSaveVisaProducts`, `fbSaveVisaProc`, `fbGetVisaProc`, `fbDeleteVisaProc`, `fbSubscribeVisaProcs`. Mirror legacy 489-506.
- [ ] Commit: `feat(firebase): visa products + procedures (PR Visa Task 2)`

---

## Task 3: Stores

**Files:**
- Create: `src/stores/visaProductsStore.ts`
- Create: `src/stores/visaProcStore.ts`
- Modify: `src/components/shell/MainApp.tsx`

- [ ] `visaProductsStore.ts` — `{ products: VisaProduct[]; rates: Record<string,number>; loading: boolean; init: () => Unsubscribe; save: (data, savedBy) => Promise<void> }`.
- [ ] `visaProcStore.ts` — `{ list: VisaProcIndexEntry[]; loading: boolean; init: () => Unsubscribe; save: (d, savedBy) => Promise<void>; load: (id) => Promise<VisaProcDoc | null>; delete: (id) => Promise<void> }`.
- [ ] Wire both `init()` in MainApp.
- [ ] Commit: `feat(visa): visaProductsStore + visaProcStore (PR Visa Task 3)`

---

## Task 4: VisaCatalog

**Files:**
- Create: `src/components/visa/VisaCatalog.tsx`

- [ ] Search input, output-currency selector (15 currencies), 💱 FX panel toggle, "+ Thêm sản phẩm visa".
- [ ] Product list: each card with country (datalist autocomplete from existing) / type / validity / location / active toggle / delete; fees list (name/perPax/amount/cur/delete); markup picker; live preview of unit + per-group prices; note input.
- [ ] All mutations through `visaProductsStore.save({products, rates}, savedBy)`. `useRestaurantStore`-style save-then-push pattern.
- [ ] Commit: `feat(visa): VisaCatalog (PR Visa Task 4)`

---

## Task 5: VisaProcHome + Manager

**Files:**
- Create: `src/components/visa/VisaProcHome.tsx`
- Create: `src/components/visa/VisaProcManager.tsx`

- [ ] Manager applies visibility filter (own || collab || CEO), passes filtered list to Home.
- [ ] Home: search + card grid (code chip + title/country + creator + linkedQuoteName + updated) + delete confirm. Delete button shown only when `createdByUsername === currentUser.u`; collaborators see a "Cộng tác" badge.
- [ ] Commit: `feat(visa): VisaProcHome + Manager (PR Visa Task 5)`

---

## Task 6: VisaProcBuilder

**Files:**
- Create: `src/components/visa/VisaProcBuilder.tsx`

- [ ] Local state for the in-progress doc; auto-save 1.5s. Header: code display, 📄 Word (stub for Task 8), 📑 PDF (stub for Task 9), 🕐 Phiên bản (count), 💾 Lưu phiên bản, 👥 Cộng tác (owner-only), ↩ Quay lại.
- [ ] Info card: title, country, linked-quote dropdown.
- [ ] Section list: each section with kind icon + editable title + delete + body. Body branches on `repeatable`:
  - **repeatable**: HTML table with STT + editable column headers + per-row cells + delete-row, "+ Thêm người" below.
  - **single**: grid of label+value pairs (`s.rows[0].values[fieldId]`), "+ Thêm trường" below.
- [ ] Field ops: add column, edit column label, delete column (clears values).
- [ ] "+ Thêm mục hồ sơ" appends a new custom section.
- [ ] Versions modal: list of `versions[]` with restore button; "💾 Lưu phiên bản" pushes a snapshot (max 10).
- [ ] Stub the Word / PDF buttons to `window.alert('Sẽ có trong task tiếp theo.')`.
- [ ] Commit: `feat(visa): VisaProcBuilder with auto-save + versions (PR Visa Task 6)`

---

## Task 7: VisaProcCollabModal

**Files:**
- Create: `src/components/visa/VisaProcCollabModal.tsx`

- [ ] Read users from `useAuthStore`. Show checkbox list excluding the owner. Click → toggle `collaborators[]`.
- [ ] Wire into VisaProcBuilder.
- [ ] Commit: `feat(visa): VisaProcCollabModal (PR Visa Task 7)`

---

## Task 8: exportVisaProcDocx

**Files:**
- Create: `src/lib/exports/exportVisaProcDocx.ts`

- [ ] Port legacy 7798-7856. Skip the logo image. Text-only brand header.
- [ ] Wire 📄 Word button in Builder.
- [ ] Commit: `feat(exports): exportVisaProcDocx (PR Visa Task 8)`

---

## Task 9: exportVisaProcPDF

**Files:**
- Create: `src/lib/exports/exportVisaProcPDF.ts`

- [ ] Port legacy 7858-7932. Helvetica + ASCII-stripped Vietnamese. Skip the logo addImage.
- [ ] Wire 📑 PDF button in Builder.
- [ ] Commit: `feat(exports): exportVisaProcPDF (PR Visa Task 9)`

---

## Task 10: VisaApp + TemplateSelector wiring

**Files:**
- Create: `src/components/visa/VisaApp.tsx`
- Modify: `src/types/quote.ts` (Template union)
- Modify: `src/components/quote/constants.ts` (TEMPLATES.visa)
- Modify: `src/components/quote/TemplateSelectorModal.tsx` (remove `visa` from coming-soon)
- Modify: `src/components/quote/QuoteView.tsx` (route to VisaApp)
- Modify: `src/components/quote/QuoteHistoryView.tsx` (TEMPLATE_LABEL)

- [ ] VisaApp: 2-tab strip (📋 Danh mục giá / 🗂️ Hồ sơ thủ tục). When a procedure is opened, render the Builder instead of the tabs.
- [ ] Extend Template, add TEMPLATES.visa, remove from coming-soon, route in QuoteView.
- [ ] Build to confirm.
- [ ] Commit: `feat(visa): wire as first-class template (PR Visa Task 10)`
