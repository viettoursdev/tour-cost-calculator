# Cloudflare Worker config cho phần dịch/OCR (KHÔNG liên quan tới website GitHub Pages).
# Cloudflare Workers Builds đọc file này khi chạy `npx wrangler deploy`.
name = "viettours-ai"
main = "cloudflare-worker/viettours-ai-worker.js"
compatibility_date = "2025-06-01"
